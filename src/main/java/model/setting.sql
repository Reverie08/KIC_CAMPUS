create table gt_member (
userid varchar(20) primary key, 
pw varchar(20), 
name varchar(20),
birth varchar(10),
gender number(1), 
phone varchar(20), 
email varchar(50), 
-- user_regdate date(),
address varchar(200) 
);

